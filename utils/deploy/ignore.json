{
  "description": "Rsync exclude patterns used by utils/deploy/deploy.sh. Mirrors the 'ignore' list in .vscode/sftp.json — keep the two in sync. Patterns without a leading slash match the basename at any depth; trailing slash restricts to directories. NOTE: .htaccess is intentionally NOT excluded — it is this site's redirect and must be deployed.",
  "patterns": [
    "_*",
    ".DS_Store",
    ".env",
    ".env.*",
    ".git",
    ".git*",
    ".claude",
    ".github",
    ".vscode",
    "authorized_keys",
    "docs/",
    "utils/",
    "node_modules/",
    "*.md",
    "*.scss",
    "*.css.map"
  ]
}
